import { HighlightOnHover } from './highlight-on-hover';

describe('HighlightOnHover', () => {
  it('should create an instance', () => {
    const directive = new HighlightOnHover();
    expect(directive).toBeTruthy();
  });
});
